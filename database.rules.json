{
  "rules": {
    "amecoSpotPlanner": {
      ".read": "auth != null",
      ".write": "auth != null",
      "workers": {
        "$workerId": {
          ".validate": "newData.hasChildren(['id','nombre','estadoLaboral'])"
        }
      },
      "sites": {
        "$siteId": {
          ".validate": "newData.hasChildren(['id','nombre','activa'])"
        }
      }
    }
  }
}